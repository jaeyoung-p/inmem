# Step 2 Notes

- This is the stock-style single-memory baseline.
- The local host must allow KVM through `/dev/kvm`.
- The Ubuntu workload uses the local numactl-enabled disk image.
- DDR5, large-memory ranges, E820 changes, SRAT/SLIT, and NUMA validation start
  in later steps.
