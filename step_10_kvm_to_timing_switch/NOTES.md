# Step 10 Notes

- KVM boot stats are not measurement data.
- Keep post-switch shell work small; shell commands under Timing CPU are slow.
- The exact detailed CPU model can change later, but the ROI boundary should
  remain explicit.
- Checkpointing is useful but not part of this first workflow.
