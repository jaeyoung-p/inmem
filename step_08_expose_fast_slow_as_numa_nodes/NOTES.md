# Step 8 Notes

- The fixed scope is exactly two NUMA nodes.
- SRAT memory affinity flags use enabled records.
- SLIT distances are symmetric: local 10, remote 20.
- Kernel parsing is validated in Step 9; this step validates ACPI object
  construction and config packaging.
